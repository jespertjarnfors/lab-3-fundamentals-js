/* function getGreeting(name) {
    return 'Hello ' + name + '!';
    } */

const getGreeting = (name) => 'Hello ' + name + '!';

console.log(getGreeting('Jesper'));

let getGreeting_1 = function (name) {
    return 'Hello ' + name + '!';
}

console.log(getGreeting_1('Mirza'));